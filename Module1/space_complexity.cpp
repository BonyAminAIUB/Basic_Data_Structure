#include <bits/stdc++.h>
using namespace std;

int main(){
    int n; // space complexity O(1)
    double d; // space complexity O(1)
    cin >> n;
    int a[n]; // space complexity O(N)
    int arr[n][n]; // space complexity O(N*N)
    return 0;
}